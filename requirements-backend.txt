# Backend
fastapi==0.109.2
uvicorn[standard]==0.27.1

# LangChain
langchain==0.3.27
langchain-core==0.3.72
langchain-community==0.3.27
langchain-ollama==0.2.3
# Faltava: a partir do langchain-community 0.3.x, HuggingFaceEmbeddings é
# só um shim que importa de langchain_huggingface por baixo dos panos.
# Sem esse pacote instalado, VectorStoreManager() quebra na inicialização
# (e derruba o startup inteiro do FastAPI, já que main.py instancia
# RAGEngine() -> VectorStoreManager() no lifespan).
langchain-huggingface==0.1.2

# Banco vetorial
chromadb==0.5.5

# Embeddings
sentence-transformers==3.0.1

# Leitura de PDFs
pypdf==4.3.1

# Modelos
transformers==4.44.2
# torch é instalado separadamente no Dockerfile.backend via índice
# cpu-only do PyTorch, para não puxar binários CUDA desnecessários.
# Mantido aqui para quem instala localmente sem Docker (dev/scraping).
torch==2.3.1